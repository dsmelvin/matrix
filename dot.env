export INFERENCE_MODEL=qwen/qwen3.5-9b
export INFERENCE_BASE_URL=http://127.0.0.1:11434/v1
export INFERENCE_API_KEY=

export AGENT_MAX_MESSAGE=1000
export AGENT_MAX_COMPLETION_TOKEN=262144

# If you keep the following settings empty, it will be like AI ChatBot instead of AI Assistant.
# Use the system prompt that works best for you or leave it empty.
# Only keep the tools you need. Otherwise, it is just token waste.
# TaskTool is the "SUBAGENT" tool.
export AGENT_PROMPT_SYSTEM=workspace/prompt/OPERATOR_SYSTEM_PROMPT_V0.md
export AGENT_PATH_MEMORY=workspace/memory
export AGENT_PATHS_AGENTS=workspace/agents
export AGENT_PATHS_SKILLS=workspace/skills
export MCP_SERVERS_CONFIGURATION=workspace/mcp-servers.json
export AGENT_TOOLS=TaskTool,SkillTool,ImageReaderTool,GrepTool,GlobTool,ShellTools,FileSystemTools,ListDirectoryTool,SmartWebFetchTool,TodoWriteTool
